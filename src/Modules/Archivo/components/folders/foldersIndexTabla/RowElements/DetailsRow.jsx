import React from "react";
import Button from "@mui/material/Button";
import { TextField } from "@mui/material";

export const DetailsRow = ({ row, handleOpenShowHistoryModal }) => {
  return (
    <div className="flex">
      <div>
       
      </div>
    </div>
  );
};
